(function() {
    'use strict';

    module.exports = LeaveYourMarkController;

    LeaveYourMarkController.$inject = [
        '$rootScope',
        '$state'
    ]

    LeaveYourMarkController.$inject = [
        '$rootScope',
        '$log',
        '$state'
    ];

    function LeaveYourMarkController(
        $rootScope,
        $log,
        $state
    ) {
        $log.debug('Inside Leave Your Mark Controller');

        var vm = this;

    }
})();
